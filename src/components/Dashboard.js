import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Bienvenue dans le panel D'administration de vos clients" />
        <CardContent>Ici vous pouvez gérer les données de vos clients !</CardContent>
    </Card>
);